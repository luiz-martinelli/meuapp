import 'package:flutter/material.dart';

class ChartHorizontal extends StatelessWidget {
  const ChartHorizontal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
